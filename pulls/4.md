README had empty `<img>` placeholders for model performance charts. The script also lacked a standalone ROC curve plot and conflated confusion matrix + feature coefficients into a single figure.

## 💡 Business Value

This model demonstrates how machine learning can be used to:
- Predict survival outcomes using structured data
- Assist decision-making in risk analysis systems
- Serve as a foundation for real-world classification problems

## Changes

### `titanic_model.py`
- Added `roc_curve` to sklearn imports
- Split monolithic 2-subplot figure into three focused, separately saved plots:
  - `confusion_matrix.png` — labeled axes (Not Survived / Survived), consistent contrast logic
  - `roc_curve.png` — FPR vs TPR with AUC annotated in legend, random classifier baseline
  - `titanic_results.png` — feature coefficients chart (retained, now standalone)

### `README.md`
- Added **💡 Business Value** section explaining ML use cases
- Added **📊 Model Performance** section embedding `confusion_matrix.png` and `roc_curve.png`

### Demo assets
- Committed `confusion_matrix.png` and `roc_curve.png` generated from synthetic data matching reported model performance (~81% accuracy, ~0.86 AUC) so the README renders immediately without needing `train.csv`